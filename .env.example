# Email configuration for Solio results
# Copy this file to .env and fill in your credentials

# Your email address (used as both sender and recipient)
EMAIL_ADDRESS=your.email@gmail.com

# =============================================================================
# OPTION A: Gmail API (Recommended - faster and more reliable)
# =============================================================================
# No password needed! Just set up Gmail API credentials:
# 1. Follow the Gmail API setup in README.md
# 2. Save credentials.json to credentials/credentials.json
# 3. Run: uv run solio-gmail-setup

# =============================================================================
# OPTION B: SMTP with App Password (fallback if Gmail API not configured)
# =============================================================================
# Your email password or app password
# For Gmail, create an App Password at: https://myaccount.google.com/apppasswords
# EMAIL_PASSWORD=your-16-char-app-password

# Optional: Override SMTP settings (defaults are for Gmail)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
